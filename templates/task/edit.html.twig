{% extends 'base.html.twig' %}

{% block title %}Modifier la tâche{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier la tâche</h1>

    <div id="task_group">



    {{ include('task/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="d-flex justify-content-between w-100 mt-2" style="grid-gap:1rem">
    <a href="{{ path('app_home_index') }}" class="btn btn-secondary flex-grow-1 ">Retour à la liste des tâches</a>

    <div class="">{{ include('task/_delete_form.html.twig') }}</div>

    </div>

    </div>
{% endblock %}
